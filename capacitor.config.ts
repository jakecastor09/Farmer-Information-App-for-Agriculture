import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.farmer-information-app-for-agriculture-in-tabon.app',
  appName: 'farmer-information-app-for-agriculture',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
